#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

  
    int n;
    scanf("%d", &n);
    int a[n];
    int *x;
    x = &a[n];
    for(int i=0; i<n; i++){
        scanf("%d", &a[n]);
        printf("%d ", *x);
    }
        return 0;
    }
