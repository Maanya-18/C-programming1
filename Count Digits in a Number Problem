#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n,digits=0;
    scanf("%d",&n);
    if(n==0){
        digits++;
    }
    while(n!=0){
        n=n/10;
        digits=digits+1;
    }
    printf("%d", digits);
    return 0;
}
