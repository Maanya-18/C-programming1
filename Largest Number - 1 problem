#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int x,y;
    
    scanf("%d %d ",&x ,&y );
    if(x>=y ){
        printf("%d\n",x);
    }
     else {
        printf("%d\n",y);
    }
    
    return 0;
}
